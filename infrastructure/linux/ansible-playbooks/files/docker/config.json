If your servers pull Docker container images from a private container registry,
you need to generate a docker config.json based on a service account that has
appropriate permissions to pull the images.

# CHANGE: Add the config.json file here and encrypt with Ansible Vault.
